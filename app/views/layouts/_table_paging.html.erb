  <tfoot>
    <tr>
      <th>
      	<%= select_tag :per_page, options_for_select([15,30,60], params[:per_page].to_i), :class=> "ui compact dropdown", :onchange => "if(this.value){window.location='?per_page='+this.value;}" %> 
      	<%= page_entries_info @carriers %> 
  	  </th>
      <th colspan="<%= @carriers.columns.count - 3 %>">
      	<%= will_paginate @carriers, renderer: WillPaginateSemanticUi::ActionView::Renderer, class: "right floated mini", inner_window: 3 %> 
      </th>
    </tr>
  </tfoot>
