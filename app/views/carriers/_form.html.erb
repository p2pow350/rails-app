<%= simple_form_for(@carrier) do |f| %>

  <%= f.input :name %>
  <%= f.input :email %>  
  <%= f.input :is_customer, as: :boolean, wrapper: :ui_checkbox %>
  <%= f.input :is_supplier, as: :boolean, wrapper: :ui_checkbox %>
  
  <%= f.button :button, "Save" %>
  <%= link_to 'Back', url_for(:back), class: "ui button" %>
<% end %>
