<div class="ui secondary pointing fluid menu">
<h2 class="ui header">Listing Carriers</h2>
	<div class="right menu"><%= render 'layouts/page_action_buttons' %></div>
</div>

<table class="ui celled compact striped table">
  <thead>
	<tr>
	  <th>Name</th>
	  <th>Is customer</th>
	  <th>Is supplier</th>
	  <th width="30px"></th>
	</tr>
  </thead>

  <tbody>	  
	<%  @carriers.each do |carrier| %>
	  <tr class="collapsing">
		<td><%= carrier.name %></td>
		<td><%= carrier.is_customer %></td>
		<td><%= carrier.is_supplier %></td>
		<td>                                                                                                                                                                
			<div class="ui tiny icon buttons">                                                                                                                                   
			  <%= link_to '<i class="edit icon"></i>'.html_safe, edit_carrier_path(carrier), class: 'ui button' %>                                                          
			  <%= link_to '<i class="delete icon"></i>'.html_safe, carrier, method: :delete, data: { confirm: 'Are you sure?' }, class: 'ui button red' %>                  
			</div>                                                                                                                                                          
		</td> 			
	  </tr>
	<% end %>
  </tbody>
  <tfoot><tr><th colspan="4"><%= paginate @carriers %></th></tr></tfoot>
</table>
  
</div>