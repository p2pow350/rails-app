# ---
# General scripts
#console.log 'Application Started'

#if $('.ui.sidebar').length
#  alert 'ciao'

$ ->
  dataConfirmModal.setDefaults
    title: 'Confirm your action'
    commit: 'Continue'
    cancel: 'Cancel'

    
$ ->
  if $('.ui.dropdown').length
  	  $('.ui.dropdown').dropdown()
  	  return

$ ->
  $('#toggle_menu').click ->
    $('.menu.sidebar').sidebar 'toggle'
    return